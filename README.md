# cpu-gpu-temperature-checker
 This Python-powered program is designed to check the CPU and GPU temperatures of a user's computer.  It notifies the user if any of the temperatures exceeds a safe threshold
